pipline {
    agent {label 'maven'}

    environment{
        PATH = "/opt/apache-maven-3.9.4/bin:$PATH"
    }
    stages{
        stages("maven_code_build"){
            steps{
                sh 'mvn clean deploy' 
            }
        }
    }
}